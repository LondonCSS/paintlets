import { showSample } from "../../_lib/show-sample";

const sampleProps = [
  "background-color",
  "background-image",
  "--tile-size",
  "--stroke-width",
  "--stroke-colour",
];

showSample("truchet", sampleProps);
