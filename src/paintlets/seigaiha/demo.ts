import { showSample } from "../../_lib/show-sample";

const sampleProps = [
  "background-color",
  "background-image",
  "--stroke-width",
  "--stroke-colour",
  "--colours",
  "--rings",
  "--radius",
];

showSample("seigaiha", sampleProps);
