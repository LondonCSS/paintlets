import { showSample } from "../../_lib/show-sample";

const sampleProps = [
  "background-color",
  "background-image",
  "--grid-unit",
  "--line-width",
  "--line-frequency",
  "--line-colour",
];

showSample("contour", sampleProps);
